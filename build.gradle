repositories {
    mavenCentral()
}

allprojects {
    group = 'com.band-it'
    version = '0.0.1-SNAPSHOT'

    ext {
        springCloudVersion = 'Greenwich.RELEASE'
    }
    
    repositories {
        mavenCentral()
        maven { url 'https://repo.spring.io/milestone' }
    }
}